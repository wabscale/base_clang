---
- hosts: all
  vars_files:
    - ../../defaults/main.yml
  vars:
    DTSVER: 7
    CLANG: 10
  roles:
    - role: base_clang
      vars:
        collections_enabled: true
        llvm_compilation: true
      tags: 'clang'
