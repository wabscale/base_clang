---
# Molecule managed
command:
  clang --version:
    exit-status: 0
    stdout:
      - 'Target: x86_64-unknown-linux-gnu'
  lld:
    exit-status: 1
    stderr:
      - lld is a generic driver.
  lldb:
    exit-status: 0
